// All shell-side geometry/transform helpers moved into the Rust cores (path bboxes via scene-core
// `objectWorldAabb`, world↔screen via the renderer projection bridge). No TS geometry math remains.

export {};
